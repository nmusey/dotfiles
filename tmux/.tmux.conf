# Change keybinds
unbind C-b
set-option -g prefix C-a
bind-key C-a send-prefix

unbind '"'
unbind %

bind Y split-window -h
bind U split-window -v

bind h select-pane -L
bind j select-pane -D
bind k select-pane -U
bind l select-pane -R

bind r source-file ~/.tmux.conf

# Window options
set -g mouse on
set -g default-terminal "screen-256color"
set-option -g allow-rename off
set -g base-index 1
set -g pane-base-index 1
set -g set-titles off

# Theme
set -g status-position top
set -g window-status-format ' #I '
set -g window-status-current-format '#[fg=color2]<#I>'
set-option -g status "on"
set-option -g status-style bg=colour237,fg=colour223
set-option -g status-justify "left"
set-option -g status-right "#{actual_song} | #{pomodoro_status} | %a %b %e - %H:%M"
set-option -g status-right-length "100"
set-option -g status-interval 1

set-option -g default-shell /usr/bin/zsh

# Plugin Settings
set -g @pomodoro-on " #[fg=color9]  "
set -g @pomodoro-off " #[fg=color2] "

# Plugins
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-resurrect'
set -g @plugin 'tmux-plugins/tmux-continuum'
set -g @plugin 'olimorris/tmux-pomodoro-plus'
set -g @plugin 'alexchaichan/tmux-spotify-tui'
set -g @plugin 'tmux-plugins/tmux-sidebar'

run '~/.tmux/plugins/tpm/tpm'
