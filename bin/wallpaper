#!/usr/bin/env bash
set -euo pipefail

if [[ $# -lt 1 ]]; then
    echo "Usage: wallpaper <image>" >&2
    exit 1
fi

img="$1"

awww img "$img"
wal --cols16 -i "$img" -o ~/.config/wal16/hooks/fuzzel.sh
